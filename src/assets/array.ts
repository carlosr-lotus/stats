/**
 * Sorts an array of numbers in ascending order.
 * @param arr - The array of numbers to sort.
 * @returns The sorted array.
 */

export function sort(arr: number[]): number[] { return arr.sort((a, b) => a - b) }